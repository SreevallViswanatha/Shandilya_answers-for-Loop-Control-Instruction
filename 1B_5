// WAP to accept a number and display all the numbers from 1 to that number.

#include <stdio.h>
#include <stdlib.h>
main()
{
    int number, n=1;

    printf("Enter a number:");
    scanf("%d", &number);

    while(n<=number)
    {
        printf("%d\n", n);

        n=n+1;
    }
}
